# SiKIdle - Dependencias del Proyecto
# Stack moderno optimizado para desarrollo y distribución

# Framework UI principal
kivy==2.3.1

# Dependencias de imágenes y multimedia
Pillow>=10.0.0

# Para Android (solo cuando se compile APK)
# pyjnius - se instala automáticamente con buildozer

# Base de datos (incluida en Python estándar)
# sqlite3 - incluida en Python

# Herramientas de desarrollo y calidad (opcional)
ruff>=0.4.4
mypy>=1.10.0

# Dependencias de empaquetado (instalar por separado)
# pyinstaller>=6.0.0  # Para Windows EXE
# buildozer>=1.5.0    # Para Android APK

# Compilación para Android
buildozer>=1.5.0
cython>=0.29.0

# Testing
pytest>=7.2.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.20.0
pytest-xdist>=3.3.0
